/* ── Events Data Service Functions ───────────────────────────────── */
import { 
  getEvents, 
  getEventBySlug, 
  getEventsByCategory, 
  getUpcomingEvents,
  getFeaturedEvents,
  EVENT_CATEGORIES
} from "@/data/events";

/* ── Re-export data functions ───────────────────────────────────── */
export { 
  getEvents, 
  getEventBySlug, 
  getEventsByCategory, 
  getUpcomingEvents,
  getFeaturedEvents,
  EVENT_CATEGORIES
};
